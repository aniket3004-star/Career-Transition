Work in the GitHub repo aniket3004-star/Career-Transition on branch main or a v1-alpha branch with a PR back to main.

Read AGENTS.md and V1_ACCEPTANCE.md first. If those files are not in the repo yet, treat the copies provided with this prompt as source of truth and add them.

Implement V1-alpha only:
- fail-closed birth-input validation (code already started in src/validation/input_contract.py)
- CLI that validates an input envelope and returns JSON + status flags
- tests and CI green
- README run instructions
- update PROJECT_STATUS.md and write V1_HANDOFF.md

Keep working in a loop: change → test → commit → next unchecked box.
Do not ask me to type continue.
Stop when every box in V1_ACCEPTANCE.md is checked with evidence.

Do not calculate charts, Dasha, transits, or career outlook.
Do not invent the missing task register #1-45.
Do not claim the historical ZIP artifacts are in this repo — they are not.
Do not deploy.

If blocked, write the blocker in PROJECT_STATUS.md and finish every unblocked V1-alpha item anyway.
