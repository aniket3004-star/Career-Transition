[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "career-transition"
version = "0.1.0"
description = "Fail-closed input validation prototype for a career-transition astrology tool"
requires-python = ">=3.11"
dependencies = []

[tool.career-transition]
test-dependencies = "stdlib-only"
