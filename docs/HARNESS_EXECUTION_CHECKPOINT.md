# Harness Execution Checkpoint

Date: 2026-09-19

## Executed

### Harness package
`career-transition-v1-validation-harness-v33.zip`

### Command
`python run_regression.py` (after extracting the ZIP and placing its expected fixture directory at `/mnt/data/v33/golden`, as the script hardcodes that path).

### Observed result
- Cases: 20
- Failures: 0
- Result: `ok: true`

This is a successful run of the packaged regression script against its 20 generated synthetic fixtures in the available environment.

## What this does establish
- The v33 regression script executed successfully in this environment.
- The script's recomputed Swiss Ephemeris longitudes, Ascendant, config assertions, and Nakshatra/pada checks matched the fixture values within its encoded thresholds for these 20 cases.

## What it does not establish
- It is not an independent provider-vs-reference test: the fixtures are generated by the companion generator using the same Swiss Ephemeris library/conventions that the regression runner uses. This is primarily a self-consistency/regression check.
- It does not validate external provider payloads, historical timezone database resolution, Dasha timing, transit ingress/egress, career rules, predictive validity, or production deployment.
- The runner uses a hard-coded absolute fixture path and converts local time to UTC using a numeric offset (`tz`), rather than demonstrating historical IANA timezone resolution. This limits portability and timezone assurance.
- This run was not a complete project test suite and does not verify later artifact versions or integration with the GitHub repository.

## Status
`SYNTHETIC_REGRESSION_EXECUTED_PASS; EXTERNAL_VALIDATION_PENDING`
